includes:
  - layer:basic
  - layer:apt
  - layer:snap
  - layer:leadership
  - layer:coordinator
  - interface:pgsql
options:
  basic:
    use_venv: true
    include_system_packages: true
    packages:
      - python3-psycopg2
  coordinator:
    log_level: debug
repo: git+ssh://git.launchpad.net/postgresql-charm
